<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./css/style.css" />
    <title>Signup</title>
  </head>
  <body>
    <%- include('navbar') %>
    <div class="grid grid-cols-6">
      <div class="col-start-3 col-span-2">
        <p class="py-2 text-xl my-2">Edit Profile</p>
        <form action="/editProfile" method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <div class="py-3">
            <label for="firstName" class="font-semibold my-2"
              >First Name:
            </label>
            <input
              type="text"
              name="firstName"
              class="border border-gray-600 text-gray-900 rounded p-2 w-full"
              value="<%= firstName  %>"
            />
          </div>
          <div class="py-3">
            <label for="lastName" class="font-semibold my-2">Last Name: </label>
            <input
              type="text"
              name="lastName"
              class="border border-gray-600 text-gray-900 rounded p-2 w-full"
              value="<%= lastName  %>"
            />
          </div>
          <div class="py-3">
            <label for="email" class="font-semibold my-2">email</label>
            <input
              type="email"
              name="email"
              class="border border-gray-600 text-gray-900 rounded p-2 w-full"
              value="<%= email  %>"
            />
          </div>
          <div class="py-3">
            <label for="password" class="font-semibold my-2"
              >To change password</label
            >
            <a
              class="text-teal-800 font-medium underline"
              href="/changePassword"
              >click here</a
            >
          </div>
          <div class="py-3">
            <button
              type="submit"
              class="text-white bg-cyan-500 hover:text-cyan-800 font-medium rounded px-5 py-2"
            >
              Edit
            </button>
          </div>
        </form>
        <div class="text-grey-600">
          <% if (messages && messages.error && messages.error.length > 0) { %>
          <% for (var i = 0; i < messages.error.length; i++) { %>
          <li><%= messages.error[i] %></li>
          <% } %> <% } else if (messages && messages.success &&
          messages.success.length > 0) { %> <% for (var i = 0; i <
          messages.success.length; i++) { %>
          <li class="text-green-500"><%= messages.success[i] %></li>
          <% } %> <% } %>
        </div>
      </div>
    </div>
  </body>
</html>
