<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="./css/style.css" />
    <title>Login</title>
    
  </head>
  <body>
    <div class="grid grid-cols-6">
      <div class="col-start-3 col-span-2">
        <%- include('header') %>
        <p class="mt-2 text-xl py-2">Welcome to VJIT Sports Scheduler</p>
        <div class="mb-2 text-xl">
        <a href="/" class="text-green-600 text-xl my-2 py-2">Home</a></div>
        <span class="text-grey-700 my-3 text-xl">Don't have an account ?</span><span><a href="/signup" class="text-xl text-green-600 ml-3">Sign up</a></span>
        <h6 class="my-3 text-xl font-semibold ">Signin</h6>
        <form action="/signin" method="post">
          <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
          <div class="py-3">
            <label for="email"><p class=" font-semibold mb-2">Email</p> </label>
            <input
              type="email"
              name="email"
              class="border border-gray-600 text-gray-900 rounded p-2 w-full"
            />
          </div>
          <div class="py-3">
            <label for="password"><p class=" font-semibold mb-2">Password</p></label>
            <input
              type="password"
              name="password"
              class="border border-gray-600 text-gray-900 rounded p-2 w-full"
            />
          </div>
          <div class="py-3">
            <button
              type="submit"
              class="text-white bg-green-600 hover:bg-green-800 font-medium rounded px-5 py-2"
            >
              Sign in
            </button>
          </div>
        </form>
        <div class="text-grey-600"><% if(messages && messages.error && messages.error.length > 0)  { %>
          <% for(var i=0; i<messages.error.length; i++) { %>
                  <li > <%= messages.error[i] %></li>
          <% } %>
        <% } %></div>
      </div>
    </div>
  </body>
</html>
